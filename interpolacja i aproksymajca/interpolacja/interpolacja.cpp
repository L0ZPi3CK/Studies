
int main()
{
	// INTERPOLACJA
	/*
	// inter_tryg
	const int nT = 3;
	const int m = 2 * nT + 1;
	// interp_lag
	const int n = 3;

	// interp_lag
	double x[n]{ -3.78, 1.36, 6.71 };
	double y[n]{ 3.21,3.6,-1.88 };
	//inter_tryg
	double xT[m] = { 0 };
	double yT[m] = { 0 };
	double a[m] = { 0 };

	// interp_lag
	//interp_lag(n,x,y);

	//inter_tryg
	matrixD X, XT;
	pamiecNew(X, m, m);
	pamiecNew(XT, m, m);

	inter_tryg(nT, m, xT, yT, X, XT, a);
	*/



	return 0;
}


